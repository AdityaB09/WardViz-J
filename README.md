# WardViz-J — Interactive Patient Storyboard from Unstructured Notes

## Quick start
```bash
docker compose build
docker compose up -d
# web: http://localhost:5173  | api: http://localhost:8080/api/health
